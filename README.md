# Uncommon HTML Bug: Incorrect DOM Element Access

This repository demonstrates a subtle but important error in accessing a DOM element using JavaScript within an HTML file.  The incorrect approach leads to unexpected behavior or a runtime error.

The bug is explained in detail in the `bug.html` file and a corrected version is provided in `bugSolution.html`.